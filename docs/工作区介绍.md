# ChiseLLM 工作区介绍

> 📅 更新日期: 2025-11-25  
> 🎯 项目目标: 利用 LLM 进行硬件代码生成与自我修复

---

## 📋 项目概述

**ChiseLLM** 是一个探索性项目，致力于将大型语言模型 (LLM) 应用于基于 Chisel (Scala) 的硬件敏捷开发。项目的核心理念是通过"反射环境"实现 **编译→阐述→仿真** 三阶段自动化验证，并基于此构建高质量的 SFT 训练数据集。

### 项目阶段

| 阶段 | 状态 | 描述 |
|------|------|------|
| Stage 1 | ✅ 已完成 | 反射环境构建 - 三阶段解耦验证框架 |
| Stage 2 | ✅ 已完成 | 数据生成管线 - 批量生成 SFT 训练数据 (1万条) |
| Stage 3 | 📋 规划中 | SFT 微调与闭环评估 |

---

## 📁 目录结构

```text
ChiseLLM/
├── 📄 build.sc                 # Mill 构建配置 (仅供 reflect_env 内部使用)
├── 📄 README.md                # 项目主文档
├── 📄 requirements.txt         # Python 依赖清单
├── 📄 LICENSE                  # 开源许可证
├── 📄 quick_upload.sh          # 快速上传脚本
│
├── 📂 src/                     # 核心源代码
│   ├── reflect_env.py          # 🔥 反射环境核心模块 (v2.1)
│   └── run_reflect.py          # CLI 接口脚本
│
├── 📂 data_gen/                # 数据生成模块
│   ├── generator_V2.py         # 🔥 V2 数据生成器 (当前主力)
│   └── generator_demo.py       # 早期演示版本 (可清理)
│
├── 📂 dataset/                 # 生成的数据集
│   └── chisel_sft_dataset_*.jsonl  # SFT 训练数据 (JSONL 格式，1万条+)
│
├── 📂 docs/                    # 项目文档
│   ├── 工作区介绍.md           # 本文档
│   ├── reflect_env使用指南.md
│   ├── 数据生成器使用指南.md
│   ├── 第一阶段指南.md / 成果总结.md
│   ├── 第二阶段指南.md / 成果总结.md
│   └── ...
│
├── 📂 tests/                   # 测试文件与示例
│   ├── *.scala                 # Chisel 测试模块
│   ├── *.cpp                   # C++ Testbench
│   ├── *.v                     # 生成的 Verilog
│   └── result.json             # 验证结果
│
├── 📂 examples/                # 示例代码 (待扩充)
├── 📂 logs/                    # 运行日志
└── 📂 temp/                    # 临时文件
```

---

## 🔧 技术栈

### 硬件描述 (固定版本，与数据集一致)

| 组件 | 版本 | 用途 |
|------|------|------|
| Scala | 2.13.12 | 基础语言 |
| Chisel | 6.0.0 | 硬件构造语言 |
| Mill | 1.0.6 | 构建工具 (内部使用) |
| Verilator | 4.038+ | Verilog 仿真器 |

> ⚠️ **注意**: 版本已锁定为 Scala 2.13.12 + Chisel 6.0.0，以确保与已生成的 1 万条数据集兼容。

### Python 环境

| 组件 | 版本 | 用途 |
|------|------|------|
| Python | 3.10 | 脚本运行环境 |
| Jinja2 | - | 模板渲染 |
| tqdm | - | 进度条显示 |
| Conda 环境 | `chisel-llm` | **必须使用** |

---

## 🚀 核心模块说明

### 1. 反射环境 (`src/reflect_env.py`)

反射环境是本项目的**唯一编译和验证方式**，实现了 **三阶段解耦验证**：

```text
Chisel 代码 → [编译] → [阐述] → Verilog → [仿真] → 测试结果
              Stage 1    Stage 2            Stage 3
```

**主要功能:**

- 接收 Chisel/Scala 代码字符串
- 自动编译 (Scala compilation)
- 自动阐述 (Chisel elaboration to Verilog)
- 自动仿真 (Verilator simulation with C++ testbench)
- 返回详细的"体检报告" (JSON 格式)

**使用方式:**

```bash
# 激活环境
conda activate chisel-llm

# 编译和阐述
python src/run_reflect.py --file tests/my_module.scala

# 完整测试（含仿真）
python src/run_reflect.py --file tests/my_module.scala --testbench tests/tb_my_module.cpp
```

### 2. 数据生成器 (`data_gen/generator_V2.py`)

基于模板的批量 SFT 数据生成器，支持：

- **Level 1**: Wire/Reg 定义、基本类型
- **Level 2**: 组合逻辑 (算术、MUX、When)
- **Level 3**: 简单时序逻辑 (计数器、移位寄存器)

**使用方式:**

```bash
conda activate chisel-llm

# 生成 100 条样本，使用 4 个并行进程
python data_gen/generator_V2.py 100 4
```

**输出格式:** JSONL，每行包含 `instruction`, `input`, `output` 三字段。

---

## 📊 数据资产

| 文件 | 描述 |
|------|------|
| `dataset/chisel_sft_dataset_*.jsonl` | 生成的 SFT 训练数据 (1万条+) |
| `logs/generation_errors_*.log` | 生成过程中的错误日志 |
| `tests/result.json` | 最近一次反射验证的结果 |

---

## 📚 文档索引

| 文档 | 内容 |
|------|------|
| [reflect_env使用指南](reflect_env使用指南.md) | **反射环境 API 文档 (核心)** |
| [数据生成器使用指南](数据生成器使用指南.md) | 生成器使用说明 |
| [第一阶段指南](第一阶段指南.md) | 反射环境构建指南 |
| [第一阶段成果总结](第一阶段成果总结.md) | Stage 1 完成报告 |
| [第二阶段指南](第二阶段指南.md) | 数据生成管线指南 |
| [第二阶段成果总结](第二阶段成果总结.md) | Stage 2 进展报告 |
| [sbt_vs_mill_对比](sbt_vs_mill_对比.md) | 构建工具选型分析 |
| [AIChisel-Verilog科研指南](AIChisel-Verilog科研指南.md) | 科研方向参考 |

---

## ⚡ 快速开始

### 1. 环境准备

```bash
# 激活 Conda 环境 (必须)
conda activate chisel-llm

# 安装 Python 依赖
pip install -r requirements.txt

# 确保 Verilator 已安装
verilator --version
```

### 2. 单样本验证

```bash
conda activate chisel-llm

python src/run_reflect.py \
    --file tests/ten_timer.scala \
    --testbench tests/tb_ten_timer.cpp
```

### 3. 批量生成数据

```bash
conda activate chisel-llm

python data_gen/generator_V2.py 100 4
```

---

## ⚠️ 重要说明

### 关于 IDE 集成

本项目**不使用** VS Code Metals 插件进行 Scala/Chisel 开发，原因如下：

1. **版本兼容性**: Metals 要求较新的 Scala 版本 (2.13.14+)，但 Chisel 6.0.0 仅支持 Scala 2.13.12
2. **数据集一致性**: 已生成的 1 万条数据集基于 Chisel 6.0.0，升级版本会导致兼容性问题
3. **反射环境**: `run_reflect.py` 已提供完整的编译、阐述、仿真功能，无需 IDE 集成

### 编译方式

| ❌ 不推荐 | ✅ 推荐 |
|----------|--------|
| VS Code Metals | `python src/run_reflect.py` |
| `mill chiselllm.compile` | `python data_gen/generator_V2.py` |
| `sbt compile` | - |

---

## 🔮 后续规划

1. **模板扩展**: 增加更复杂的时序逻辑、接口协议 (Valid-Ready)、参数化模块
2. **SFT 训练**: 基于生成的数据集微调 Qwen2.5-Coder 模型
3. **闭环评估**: 构建测试集评估 Pass@1 Compile 性能

---

## 📝 贡献指南

1. 新增模板请放置在 `data_gen/` 目录
2. 测试文件请放置在 `tests/` 目录
3. 文档更新请放置在 `docs/` 目录
4. **提交前请确保通过反射环境验证**

---

本文档由 ChiseLLM 项目维护 | 更新于 2025-11-25
